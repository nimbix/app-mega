# app-mega

Mega App for testing them all.

2 versions available:

* k8s kind, for k8s scheduler and slurm scheduler
* kns kind, for kns scheduler

## Launch a test

Note that this test will launch all jobs in parallel, to emulate a surge of jobs (this could allow to see scaleup related issues).

Grab your user id and your api key from portal.

Then launch the provided script:




